---
phase: 01-infrastructure-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
  - ~/Desktop/tapzi-barber/.env
autonomous: false
requirements:
  - INFRA-03
user_setup:
  - service: livekit-cloud
    why: "LiveKit Cloud provides the WebRTC infrastructure for live streaming in Phase 4"
    account_action: "Create LiveKit Cloud account and project"
    dashboard_config:
      - task: "Sign up and create project"
        location: "https://cloud.livekit.io/"
        steps:
          - "Sign up at https://cloud.livekit.io/"
          - "Create a new project named 'tapzi' (or 'tapzi-live')"
          - "Select 'eu-central' region (closest to Romania)"
          - "Go to Settings > Keys"
          - "Copy the API Key, API Secret, and WebSocket URL (wss://<project>.livekit.cloud)"
      - task: "Store LiveKit secrets in Supabase Edge Function secrets"
        location: "Supabase Dashboard > Edge Functions > Secrets"
        steps:
          - "Go to https://supabase.com/dashboard/project/iaqztbhkukgghomwnict/settings/functions"
          - "Add secret: LIVEKIT_API_KEY = <your API key from LiveKit dashboard>"
          - "Add secret: LIVEKIT_API_SECRET = <your API secret from LiveKit dashboard>"

must_haves:
  truths:
    - "LiveKit Cloud dashboard shows an active project with API key and secret available"
    - "EXPO_PUBLIC_LIVEKIT_URL is set in both apps .env files"
    - "LIVEKIT_API_KEY and LIVEKIT_API_SECRET are stored in Supabase Edge Function secrets (NOT in app .env files)"
  artifacts:
    - path: ".env"
      provides: "LiveKit URL for barber-store"
      contains: "EXPO_PUBLIC_LIVEKIT_URL=wss://"
    - path: "~/Desktop/tapzi-barber/.env"
      provides: "LiveKit URL for tapzi-barber"
      contains: "EXPO_PUBLIC_LIVEKIT_URL=wss://"
  key_links:
    - from: ".env (both apps)"
      to: "LiveKit Cloud project"
      via: "EXPO_PUBLIC_LIVEKIT_URL"
      pattern: "EXPO_PUBLIC_LIVEKIT_URL=wss://.*\\.livekit\\.cloud"
    - from: "Supabase Edge Function secrets"
      to: "LiveKit Cloud API"
      via: "LIVEKIT_API_KEY + LIVEKIT_API_SECRET"
      pattern: "server-side only"
---

<objective>
Create a LiveKit Cloud account, set up a project, and store credentials in the correct locations (client-safe URL in .env, secrets in Supabase Edge Function secrets).

Purpose: LiveKit Cloud provides the WebRTC infrastructure for live streaming (Phase 4). The account and credentials must exist before any LiveKit integration code can be written. API secrets must stay server-side per locked decision.

Output: LiveKit Cloud project active, EXPO_PUBLIC_LIVEKIT_URL in both apps' .env, LIVEKIT_API_KEY and LIVEKIT_API_SECRET in Supabase Edge Function secrets.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-setup/01-CONTEXT.md
@.planning/phases/01-infrastructure-setup/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create LiveKit Cloud account and project</name>
  <action>
    The user must create a LiveKit Cloud account and project. This requires web signup and cannot be automated.

    **Steps:**
    1. Go to https://cloud.livekit.io/ and sign up (or log in if already have an account)
    2. Create a new project:
       - Name: `tapzi` (or `tapzi-live`)
       - Region: Select `eu-central` (closest to Romania, likely Frankfurt)
    3. After project creation, go to **Settings > Keys**
    4. Note down three values:
       - **WebSocket URL**: `wss://<project-name>.livekit.cloud` (e.g., `wss://tapzi.livekit.cloud`)
       - **API Key**: starts with `API...`
       - **API Secret**: the secret string
    5. Go to **Supabase Dashboard > Project Settings > Edge Functions > Secrets**:
       - URL: https://supabase.com/dashboard/project/iaqztbhkukgghomwnict/settings/functions
       - Add secret: `LIVEKIT_API_KEY` = your API key
       - Add secret: `LIVEKIT_API_SECRET` = your API secret

    IMPORTANT: Do NOT put LIVEKIT_API_KEY or LIVEKIT_API_SECRET in any .env file. These are server-side only secrets stored in Supabase Edge Function secrets.

    When done, provide the WebSocket URL (e.g., `wss://tapzi.livekit.cloud`) so the next task can add it to .env files.
  </action>
  <resume-signal>Provide the LiveKit WebSocket URL (e.g., "wss://tapzi.livekit.cloud") and confirm secrets are stored in Supabase</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add EXPO_PUBLIC_LIVEKIT_URL to both apps' .env files</name>
  <read_first>
    - /Users/edi/Desktop/barber-store/.env
    - /Users/edi/Desktop/tapzi-barber/.env
  </read_first>
  <files>.env, ~/Desktop/tapzi-barber/.env</files>
  <action>
    Add the LiveKit WebSocket URL (provided by user in Task 1) to both apps' .env files. Use the `EXPO_PUBLIC_` prefix since this is a client-safe value (just a WebSocket endpoint, no secrets).

    **barber-store/.env** -- append this line (do NOT modify existing lines):
    ```
    EXPO_PUBLIC_LIVEKIT_URL=wss://<project-name>.livekit.cloud
    ```
    Where `<project-name>` is replaced with the actual project name from the user's response in Task 1.

    **tapzi-barber/.env** -- append the same line:
    ```
    EXPO_PUBLIC_LIVEKIT_URL=wss://<project-name>.livekit.cloud
    ```

    The resulting barber-store/.env should contain:
    ```
    EXPO_PUBLIC_SUPABASE_URL=https://iaqztbhkukgghomwnict.supabase.co
    EXPO_PUBLIC_SUPABASE_ANON_KEY=<existing value>
    EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=<existing value>
    EXPO_PUBLIC_LIVEKIT_URL=wss://<project>.livekit.cloud
    ```

    The resulting tapzi-barber/.env should contain:
    ```
    EXPO_PUBLIC_SUPABASE_URL=https://iaqztbhkukgghomwnict.supabase.co
    EXPO_PUBLIC_SUPABASE_ANON_KEY=<existing value>
    EXPO_PUBLIC_LIVEKIT_URL=wss://<project>.livekit.cloud
    ```

    Also add `EXPO_PUBLIC_LIVEKIT_URL` to tapzi-barber's `eas.json` env blocks (development, preview, production) to match the existing pattern where env vars are duplicated in eas.json for EAS builds. Read `/Users/edi/Desktop/tapzi-barber/eas.json` first to see the current structure, then add the new var to each profile's `env` block.
  </action>
  <verify>
    <automated>grep -q "EXPO_PUBLIC_LIVEKIT_URL=wss://" /Users/edi/Desktop/barber-store/.env && grep -q "EXPO_PUBLIC_LIVEKIT_URL=wss://" /Users/edi/Desktop/tapzi-barber/.env && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/edi/Desktop/barber-store/.env` contains a line matching `EXPO_PUBLIC_LIVEKIT_URL=wss://.*\.livekit\.cloud`
    - `/Users/edi/Desktop/tapzi-barber/.env` contains a line matching `EXPO_PUBLIC_LIVEKIT_URL=wss://.*\.livekit\.cloud`
    - `/Users/edi/Desktop/tapzi-barber/eas.json` contains `EXPO_PUBLIC_LIVEKIT_URL` in the development, preview, and production env blocks
    - Neither .env file contains `LIVEKIT_API_KEY` or `LIVEKIT_API_SECRET` (these are server-side only)
    - Existing .env lines are preserved unchanged
  </acceptance_criteria>
  <done>EXPO_PUBLIC_LIVEKIT_URL is set in both apps' .env files and in tapzi-barber eas.json env blocks, pointing to the active LiveKit Cloud project. No secrets are in client-side files.</done>
</task>

</tasks>

<verification>
- LiveKit Cloud dashboard shows active project (confirmed by user)
- `grep EXPO_PUBLIC_LIVEKIT_URL /Users/edi/Desktop/barber-store/.env` shows wss:// URL
- `grep EXPO_PUBLIC_LIVEKIT_URL /Users/edi/Desktop/tapzi-barber/.env` shows same URL
- `grep -c LIVEKIT_API_KEY /Users/edi/Desktop/barber-store/.env /Users/edi/Desktop/tapzi-barber/.env` returns 0 (secrets not in .env)
</verification>

<success_criteria>
- LiveKit Cloud project exists with API key and secret (confirmed by user)
- Both apps have EXPO_PUBLIC_LIVEKIT_URL in their .env files
- LIVEKIT_API_KEY and LIVEKIT_API_SECRET are in Supabase Edge Function secrets (confirmed by user)
- No LiveKit secrets are present in any client-side .env file
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-setup/01-02-SUMMARY.md`
</output>
