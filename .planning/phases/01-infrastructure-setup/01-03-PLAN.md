---
phase: 01-infrastructure-setup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app.json
  - ~/Desktop/tapzi-barber/package.json
  - ~/Desktop/tapzi-barber/package-lock.json
  - ~/Desktop/tapzi-barber/app.json
autonomous: false
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Both apps have expo-dev-client installed"
    - "Both apps have LiveKit SDK packages installed"
    - "Both apps have LiveKit and WebRTC Expo plugins configured in app.json"
    - "Both apps have a working EAS dev build profile in eas.json"
  artifacts:
    - path: "package.json"
      provides: "barber-store dependencies including expo-dev-client and LiveKit SDK"
      contains: "expo-dev-client"
    - path: "app.json"
      provides: "barber-store Expo config with LiveKit plugins"
      contains: "@livekit/react-native-expo-plugin"
    - path: "~/Desktop/tapzi-barber/package.json"
      provides: "tapzi-barber dependencies including expo-dev-client and LiveKit SDK"
      contains: "expo-dev-client"
    - path: "~/Desktop/tapzi-barber/app.json"
      provides: "tapzi-barber Expo config with LiveKit plugins"
      contains: "@livekit/react-native-expo-plugin"
  key_links:
    - from: "app.json plugins array"
      to: "EAS dev build"
      via: "Expo config plugins compile native modules"
      pattern: "@livekit/react-native-expo-plugin"
    - from: "package.json dependencies"
      to: "app.json plugins"
      via: "Installed packages provide config plugins"
      pattern: "@config-plugins/react-native-webrtc"
---

<objective>
Install expo-dev-client and LiveKit SDK packages in both barber-store and tapzi-barber, configure LiveKit Expo plugins in both app.json files, and trigger EAS dev builds.

Purpose: LiveKit requires native WebRTC modules that cannot run in Expo Go. Both apps need dev builds with the LiveKit config plugins so that Phase 4 live streaming code works on real devices. expo-dev-client enables the development build runtime.

Output: Both apps have all required packages installed, plugins configured, and EAS dev builds triggered.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-setup/01-CONTEXT.md
@.planning/phases/01-infrastructure-setup/01-RESEARCH.md

<interfaces>
<!-- Existing app.json plugin arrays that will be modified -->

barber-store app.json plugins (current):
```json
["expo-router", "expo-secure-store", "expo-font", "expo-image-picker", ["expo-location", {...}]]
```

tapzi-barber app.json plugins (current):
```json
[["expo-camera", {...}], "expo-router", ["expo-splash-screen", {...}], "expo-secure-store", "expo-video"]
```

barber-store eas.json development profile (current):
```json
{ "developmentClient": true, "distribution": "internal", "ios": { "simulator": false } }
```

tapzi-barber eas.json development profile (current):
```json
{ "developmentClient": true, "distribution": "internal", "env": { "EXPO_PUBLIC_SUPABASE_URL": "...", "EXPO_PUBLIC_SUPABASE_ANON_KEY": "..." } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and configure plugins in both apps</name>
  <read_first>
    - /Users/edi/Desktop/barber-store/package.json
    - /Users/edi/Desktop/barber-store/app.json
    - /Users/edi/Desktop/barber-store/eas.json
    - /Users/edi/Desktop/tapzi-barber/package.json
    - /Users/edi/Desktop/tapzi-barber/app.json
    - /Users/edi/Desktop/tapzi-barber/eas.json
  </read_first>
  <files>package.json, app.json, ~/Desktop/tapzi-barber/package.json, ~/Desktop/tapzi-barber/app.json</files>
  <action>
    **Part A: Install packages in barber-store**

    Run in `/Users/edi/Desktop/barber-store`:
    ```bash
    npx expo install expo-dev-client
    npm install @livekit/react-native @livekit/react-native-webrtc livekit-client @livekit/react-native-expo-plugin @config-plugins/react-native-webrtc
    ```

    **Part B: Install packages in tapzi-barber**

    Run in `/Users/edi/Desktop/tapzi-barber`:
    ```bash
    npx expo install expo-dev-client
    npm install @livekit/react-native @livekit/react-native-webrtc livekit-client @livekit/react-native-expo-plugin @config-plugins/react-native-webrtc
    ```

    **Part C: Add LiveKit plugins to barber-store app.json**

    Read `/Users/edi/Desktop/barber-store/app.json`, then add two plugins to the `expo.plugins` array. The final plugins array must be:
    ```json
    [
      "expo-router",
      "expo-secure-store",
      "expo-font",
      "expo-image-picker",
      ["expo-location", { "locationWhenInUsePermission": "Avem nevoie de locatia ta pentru a-ti arata saloanele din apropiere." }],
      "@livekit/react-native-expo-plugin",
      "@config-plugins/react-native-webrtc"
    ]
    ```
    Do NOT modify any other fields in app.json.

    **Part D: Add LiveKit plugins to tapzi-barber app.json**

    Read `/Users/edi/Desktop/tapzi-barber/app.json`, then add two plugins to the `expo.plugins` array. The final plugins array must be:
    ```json
    [
      ["expo-camera", { "cameraPermission": "TAPZI are nevoie de camera pentru a scana coduri QR de invitatie." }],
      "expo-router",
      ["expo-splash-screen", { "image": "./assets/images/splash-icon.png", "imageWidth": 200, "resizeMode": "contain", "backgroundColor": "#ffffff", "dark": { "backgroundColor": "#000000" } }],
      "expo-secure-store",
      "expo-video",
      "@livekit/react-native-expo-plugin",
      "@config-plugins/react-native-webrtc"
    ]
    ```
    Do NOT modify any other fields in app.json.

    **Important notes:**
    - Both apps already have `newArchEnabled: true` in app.json -- leave this unchanged
    - Both apps already have `eas.json` with `developmentClient: true` in the development profile -- no changes needed to eas.json
    - The plugins must be strings (not arrays with config) since the LiveKit and WebRTC plugins require no additional configuration
  </action>
  <verify>
    <automated>cd /Users/edi/Desktop/barber-store && node -e "const p=require('./package.json'); const a=require('./app.json'); console.log('expo-dev-client:', !!p.dependencies['expo-dev-client']); console.log('livekit:', !!p.dependencies['@livekit/react-native']); console.log('plugin:', a.expo.plugins.includes('@livekit/react-native-expo-plugin'))" && cd /Users/edi/Desktop/tapzi-barber && node -e "const p=require('./package.json'); const a=require('./app.json'); console.log('expo-dev-client:', !!p.dependencies['expo-dev-client']); console.log('livekit:', !!p.dependencies['@livekit/react-native']); console.log('plugin:', a.expo.plugins.includes('@livekit/react-native-expo-plugin'))"</automated>
  </verify>
  <acceptance_criteria>
    - barber-store `package.json` contains `expo-dev-client` in dependencies
    - barber-store `package.json` contains `@livekit/react-native` in dependencies
    - barber-store `package.json` contains `@livekit/react-native-webrtc` in dependencies
    - barber-store `package.json` contains `livekit-client` in dependencies
    - barber-store `app.json` plugins array contains `@livekit/react-native-expo-plugin`
    - barber-store `app.json` plugins array contains `@config-plugins/react-native-webrtc`
    - tapzi-barber `package.json` contains `expo-dev-client` in dependencies
    - tapzi-barber `package.json` contains `@livekit/react-native` in dependencies
    - tapzi-barber `app.json` plugins array contains `@livekit/react-native-expo-plugin`
    - tapzi-barber `app.json` plugins array contains `@config-plugins/react-native-webrtc`
    - All existing plugins in both app.json files are preserved unchanged
    - No other fields in app.json are modified
  </acceptance_criteria>
  <done>Both apps have expo-dev-client, LiveKit SDK packages, and WebRTC packages installed. Both app.json files have LiveKit and WebRTC config plugins added to the plugins array.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Trigger EAS dev builds and verify they succeed</name>
  <action>
    The executor triggers EAS dev builds for both apps. Run these commands sequentially (one platform at a time to avoid build queue issues):

    ```bash
    # barber-store iOS
    cd /Users/edi/Desktop/barber-store
    eas build --platform ios --profile development --non-interactive

    # tapzi-barber iOS
    cd /Users/edi/Desktop/tapzi-barber
    eas build --platform ios --profile development --non-interactive
    ```

    Note: barber-store eas.json has `ios.simulator: false` which targets physical devices and requires an active Apple Developer Program membership. If the build fails due to signing/provisioning errors, the executor should temporarily set `ios.simulator: true` in barber-store's eas.json development profile for initial validation.

    Android builds can also be triggered if needed:
    ```bash
    eas build --platform android --profile development --non-interactive
    ```

    After triggering builds, the user must verify:
    1. Check EAS build dashboard at https://expo.dev for both projects:
       - barber-store (projectId: 736a549c-35cd-4d81-8644-72075004b1d0)
       - tapzi-barber (projectId: b45813cf-3cf3-469c-9ed7-3993133091c0)
    2. Verify builds complete without errors
    3. If iOS build succeeded: download and install on device/simulator
    4. Verify the dev build app launches (shows the Expo dev client UI, not Expo Go)
    5. If builds fail: report the error so we can fix and retry
  </action>
  <verify>
    <automated>cd /Users/edi/Desktop/barber-store && eas build:list --platform ios --limit 1 --non-interactive 2>&1 | head -5</automated>
  </verify>
  <done>At least one platform (iOS or Android) dev build succeeds for each app and the dev build app installs and launches.</done>
  <resume-signal>Type "builds passed" if at least one platform build succeeded for each app, or describe the errors</resume-signal>
</task>

</tasks>

<verification>
- `node -e "console.log(!!require('./package.json').dependencies['expo-dev-client'])"` returns `true` in both app directories
- `node -e "console.log(require('./app.json').expo.plugins.includes('@livekit/react-native-expo-plugin'))"` returns `true` in both app directories
- EAS build logs show successful completion (no native compilation errors)
- Dev build app installs and launches on device or simulator
</verification>

<success_criteria>
- expo-dev-client is installed in both apps
- LiveKit SDK packages are installed in both apps
- LiveKit and WebRTC config plugins are in both app.json files
- At least one platform (iOS or Android) dev build succeeds for each app
- Dev build installs and shows Expo dev client UI
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-setup/01-03-SUMMARY.md`
</output>
