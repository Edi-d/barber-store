---
phase: 04-live-streaming
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - /Users/edi/Desktop/tapzi-barber/app/go-live.tsx
  - /Users/edi/Desktop/tapzi-barber/app/_layout.tsx
  - /Users/edi/Desktop/tapzi-barber/lib/livekit.ts
  - /Users/edi/Desktop/tapzi-barber/hooks/use-live-broadcast.ts
autonomous: true
requirements: [LIVE-01, BARBER-01, BARBER-02, BARBER-03, BARBER-04]

must_haves:
  truths:
    - "Barber can fill in a title and tap Start Live to begin broadcasting video+audio"
    - "Camera preview shows the barber's face in real-time on the broadcast screen"
    - "Mute button toggles microphone on/off during broadcast"
    - "Flip button switches front/back camera without crashing (uses restartTrack, not switchActiveDevice)"
    - "End Stream button stops publishing, updates lives row to status=ended, and navigates back"
    - "registerGlobals() is called at tapzi-barber app startup"
  artifacts:
    - path: "/Users/edi/Desktop/tapzi-barber/app/go-live.tsx"
      provides: "Go-live setup form + live broadcast screen"
      contains: "LiveKitRoom"
    - path: "/Users/edi/Desktop/tapzi-barber/lib/livekit.ts"
      provides: "Token fetch helper for tapzi-barber"
      exports: ["fetchLiveKitToken", "LIVEKIT_URL"]
    - path: "/Users/edi/Desktop/tapzi-barber/hooks/use-live-broadcast.ts"
      provides: "Broadcast state management hook"
      exports: ["useLiveBroadcast"]
    - path: "/Users/edi/Desktop/tapzi-barber/app/_layout.tsx"
      provides: "registerGlobals() call"
      contains: "registerGlobals"
  key_links:
    - from: "/Users/edi/Desktop/tapzi-barber/app/go-live.tsx"
      to: "/Users/edi/Desktop/tapzi-barber/lib/livekit.ts"
      via: "fetchLiveKitToken call before LiveKitRoom connect"
      pattern: "fetchLiveKitToken"
    - from: "/Users/edi/Desktop/tapzi-barber/app/go-live.tsx"
      to: "supabase lives table"
      via: "insert row with status=starting, update to live on connect, update to ended on end"
      pattern: "supabase.*from.*lives"
    - from: "/Users/edi/Desktop/tapzi-barber/hooks/use-live-broadcast.ts"
      to: "livekit-client"
      via: "useLocalParticipant for mute/flip controls"
      pattern: "useLocalParticipant"
---

<objective>
Build the barber broadcast screen in tapzi-barber so barbers can go live with video and audio.

Purpose: This is the publisher side of the live streaming feature. The barber fills in a title, taps "Start Live", and the app creates a lives DB row, fetches a LiveKit token with canPublish:true, connects to the LiveKit room, and shows a full-screen camera preview with mute/flip/end controls.

Output: Working go-live.tsx in tapzi-barber with form + broadcast view, livekit.ts helper, registerGlobals
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-live-streaming/04-CONTEXT.md
@.planning/phases/04-live-streaming/04-RESEARCH.md
@.planning/phases/04-live-streaming/04-01-SUMMARY.md

@/Users/edi/Desktop/tapzi-barber/app/_layout.tsx
@/Users/edi/Desktop/tapzi-barber/hooks/use-social-feed.ts
@/Users/edi/Desktop/tapzi-barber/lib/supabase.ts

<interfaces>
<!-- From 04-01 plan outputs -->

From lib/livekit.ts (created in 04-01, copy same pattern for tapzi-barber):
```typescript
export const LIVEKIT_URL: string; // process.env.EXPO_PUBLIC_LIVEKIT_URL
export async function fetchLiveKitToken(
  roomName: string,
  canPublish: boolean
): Promise<{ token: string; serverUrl: string }>;
```

From types/database.ts (aligned in 04-01):
```typescript
// Lives table Row (matches actual DB)
{
  id: string;
  author_id: string;
  title: string;
  cover_url: string | null;
  room_name: string;
  status: LiveStatus; // "starting" | "live" | "ended"
  playback_url: string | null;
  viewers_count: number;
  started_at: string | null;
  ended_at: string | null;
  created_at: string;
}
```

From @livekit/react-native SDK:
```typescript
import { LiveKitRoom, useTracks, VideoTrack, AudioSession, useLocalParticipant } from '@livekit/react-native';
import { Track, isTrackReference } from 'livekit-client';
```

From tapzi-barber hooks/use-social-feed.ts useLiveStreams():
```typescript
// Existing hook queries lives table with profiles!host_id join
// This will need to be updated to profiles!author_id after 04-01 schema alignment
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add registerGlobals + lib/livekit.ts to tapzi-barber</name>
  <files>/Users/edi/Desktop/tapzi-barber/app/_layout.tsx, /Users/edi/Desktop/tapzi-barber/lib/livekit.ts</files>
  <action>
  1. In `/Users/edi/Desktop/tapzi-barber/app/_layout.tsx`, add at the TOP after existing imports:
     ```typescript
     import { registerGlobals } from '@livekit/react-native';
     registerGlobals();
     ```
     Place before any component definition. This must be module-level.

  2. Create `/Users/edi/Desktop/tapzi-barber/lib/livekit.ts` (same pattern as barber-store's lib/livekit.ts from plan 04-01):
     - Import supabase from `./supabase`
     - Export `LIVEKIT_URL = process.env.EXPO_PUBLIC_LIVEKIT_URL!`
     - Export `async function fetchLiveKitToken(roomName: string, canPublish: boolean): Promise<{ token: string; serverUrl: string }>`
       - Gets session via `supabase.auth.getSession()`
       - Throws if no session
       - Calls `supabase.functions.invoke('token-livekit', { body: { room_name: roomName, can_publish: canPublish } })`
       - Returns `{ token: data.token, serverUrl: LIVEKIT_URL }`

  3. Add EXPO_PUBLIC_LIVEKIT_URL to tapzi-barber's .env file if not already there. The value is: `wss://skylarkbv-l5kh1dli.livekit.cloud`

  4. Update the `useLiveStreams()` hook in `/Users/edi/Desktop/tapzi-barber/hooks/use-social-feed.ts`:
     - Change the Supabase join from `profiles!host_id` to `host:profiles!author_id` to match the actual DB column name (author_id)
     - This aligns with the schema fix from plan 04-01
  </action>
  <verify>
    <automated>cd /Users/edi/Desktop/tapzi-barber && grep -q "registerGlobals" app/_layout.tsx && test -f lib/livekit.ts && grep -q "fetchLiveKitToken" lib/livekit.ts && grep -q "author_id" hooks/use-social-feed.ts && echo "PASS"</automated>
  </verify>
  <done>tapzi-barber has registerGlobals at app entry, livekit.ts token helper, correct DB column reference in useLiveStreams.</done>
</task>

<task type="auto">
  <name>Task 2: Build go-live.tsx broadcast screen with camera + controls</name>
  <files>/Users/edi/Desktop/tapzi-barber/app/go-live.tsx, /Users/edi/Desktop/tapzi-barber/hooks/use-live-broadcast.ts</files>
  <action>
  1. Create `/Users/edi/Desktop/tapzi-barber/hooks/use-live-broadcast.ts` with a `useLiveBroadcast` hook that manages broadcast lifecycle:
     - State: `phase: 'setup' | 'connecting' | 'live' | 'ending'`
     - State: `liveId: string | null`, `roomName: string | null`, `token: string | null`
     - `startBroadcast(title: string)`:
       a. Set phase to 'connecting'
       b. Generate room_name: `live-${Date.now()}-${user.id.slice(0, 8)}`
       c. Insert lives row: `{ author_id: user.id, title, room_name, status: 'starting', started_at: new Date().toISOString() }`
       d. Call `fetchLiveKitToken(room_name, true)` to get canPublish token
       e. Store liveId, roomName, token
       f. Set phase to 'live' (LiveKitRoom component will auto-connect with the token)
     - `endBroadcast()`:
       a. Set phase to 'ending'
       b. Update lives row: `{ status: 'ended', ended_at: new Date().toISOString() }`
       c. Clear state, set phase to 'setup'
     - `onRoomConnected()`: Update lives row status from 'starting' to 'live'
     - Uses supabase from `@/lib/supabase` and fetchLiveKitToken from `@/lib/livekit`

  2. Create `/Users/edi/Desktop/tapzi-barber/app/go-live.tsx` with TWO views based on phase:

     **Setup phase (phase === 'setup'):**
     - Header with back button and "Go Live" title
     - TextInput for stream title (required, min 3 chars)
     - "Start Live" button that calls `startBroadcast(title)`
     - Follow the glassmorphism design language from tapzi-barber (dark backgrounds, blur, rounded corners)

     **Live phase (phase === 'live'):**
     - Full-screen layout using `LiveKitRoom` component:
       ```
       <LiveKitRoom
         serverUrl={LIVEKIT_URL}
         token={token}
         connect={true}
         options={{ adaptiveStream: { pixelDensity: 'screen' } }}
         audio={true}
         video={true}
         onConnected={() => onRoomConnected()}
       >
         <BroadcastContent />
       </LiveKitRoom>
       ```
     - IMPORTANT: Call `AudioSession.startAudioSession()` in useEffect when entering live phase, `AudioSession.stopAudioSession()` on cleanup
     - `BroadcastContent` component:
       - Uses `useTracks([Track.Source.Camera])` to get local camera track
       - Renders `VideoTrack` full-screen for camera preview (filter for `participant.isLocal`)
       - Overlay controls at bottom with glassmorphism background (blur + semi-transparent dark):
         - **Mute button**: Uses `useLocalParticipant()` -> `localParticipant.setMicrophoneEnabled(!isMuted)` — toggle icon between mic and mic-off
         - **Flip camera button**: Gets camera track via `localParticipant.getTrackPublication(Track.Source.Camera)`, calls `track.restartTrack({ facingMode: isFrontCamera ? 'environment' : 'user' })` — DO NOT use switchActiveDevice (iOS crash bug per GitHub issue #218)
         - **End stream button** (red): Calls `endBroadcast()` which disables camera+mic, updates DB, navigates back
       - LIVE badge (red dot + "LIVE" text) at top-left
       - Viewer count badge at top-right (show 0 for now, will be wired to Presence in 04-03/04-04)
       - Stream title displayed subtly below LIVE badge

     **Connecting phase:** Show loading spinner with "Connecting..." text

     **Navigation:** Add a route to go-live in tapzi-barber. Check if there's already a way to navigate to it from the social tab's "live" action chip — the existing code shows `handleChipPress` with an Alert placeholder for 'live'. Update that to `router.push('/go-live')`.

  3. In the social tab file (`/Users/edi/Desktop/tapzi-barber/app/(tabs)/social.tsx` or wherever handleChipPress is), replace the Alert placeholder for the 'live' action with `router.push('/go-live')`.
  </action>
  <verify>
    <automated>cd /Users/edi/Desktop/tapzi-barber && test -f app/go-live.tsx && test -f hooks/use-live-broadcast.ts && grep -q "LiveKitRoom" app/go-live.tsx && grep -q "restartTrack" app/go-live.tsx && grep -q "AudioSession" app/go-live.tsx && echo "PASS"</automated>
  </verify>
  <done>Barber can navigate to go-live from social tab, enter a title, start broadcasting with LiveKit. Camera preview displays full-screen. Mute toggles mic, flip uses restartTrack (not switchActiveDevice), end stream updates DB and navigates back. AudioSession managed correctly for iOS audio.</done>
</task>

</tasks>

<verification>
- tapzi-barber `app/go-live.tsx` exists with LiveKitRoom, VideoTrack, controls
- registerGlobals() called in tapzi-barber `_layout.tsx`
- `lib/livekit.ts` exists with fetchLiveKitToken
- `hooks/use-live-broadcast.ts` manages setup -> connecting -> live -> ending lifecycle
- Camera flip uses `restartTrack({ facingMode })` not `switchActiveDevice`
- AudioSession.startAudioSession/stopAudioSession called on live screen entry/exit
- Social tab "live" chip navigates to /go-live instead of showing Alert
- lives table insert uses `author_id` and includes `room_name`
</verification>

<success_criteria>
A barber in tapzi-barber can tap the "live" chip on the social tab, enter a title, and start broadcasting. The broadcast screen shows their camera feed full-screen with working mute, flip, and end controls. The lives table has a row with status "live" during broadcast and "ended" after stopping.
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-streaming/04-02-SUMMARY.md`
</output>
