// The marketplace is temporarily disabled. The Shop tab now renders the
// loyalty "Punctele mele" screen instead of the marketplace home.
// To re-enable the shop, restore the original re-export:
//   export { default } from '../marketplace/index';
export { default } from '../loyalty';
